#include<iostream>
using namespace std;
int main(){
    string str1="Hello, World!";
    string suubstr="World!";
    size_t found=str.find(substr);
    if(found != string::npos){
    cout<<"Substring found at index: "<<found<<endl;
    }
    else{
        cout<<"Substring not found"<<endl;
    }
    return 0;
}
